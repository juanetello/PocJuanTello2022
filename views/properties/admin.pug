extends ../layout/index 

block contenido
    div.py-10
        h1.text-4xl.my-10.font-extrabold.texte-center POC 
            span.font-normal Juan Tello 
        h2.text-center.text-2xl.font-extrabold= pagina
        input(type="hidden" name="_csrf" value= csrfToken)

        a(href="/propiedades/create" class="rounded py-2 px-10 bg-green-600 hover:bg-green-700 text-sm font-bold text-center text-white uppercase my-5 inline-block w-full sm:w-auto") Publicar propiedad